[pytest]
testpaths = tests
pythonpath = src/engine
addopts = -q --strict-markers
markers =
    engine: requires the ppt-master engine; slow (minutes per deck)
    fixtures: requires the third-party corpus (scripts/fetch_fixtures.py)
