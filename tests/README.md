# tests/

Test suites and the fidelity benchmark corpus.

```
corpus/   Benchmark decks. Only decks cleared for sharing are tracked;
          customer material never enters Git (see .gitignore).
```

Strategy and the corpus requirements: `docs/guides/testing-strategy.md`.

The rule that matters: **every real deck that breaks becomes a permanent corpus entry the same day.**
